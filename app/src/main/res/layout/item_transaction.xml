<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">
    
    <data>
        <variable
                name="viewModel"
                type="com.schweitzering.walletmanager.main.MainViewModel" />

        <variable
                name="viewHolder"
                type="com.schweitzering.walletmanager.main.TransactionsAdapter.TransactionsViewHolder" />
    </data>
    
<LinearLayout
        android:orientation="vertical" android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <TextView
            android:id="@+id/value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#000000"
            android:text="@{Float.toString(viewHolder.transaction.value)}"/>

    <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewHolder.transaction.date.toString()}"/>

    <TextView
            android:id="@+id/category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewHolder.transaction.category.name()}"/>

    <TextView
            android:id="@+id/categoryType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewHolder.transaction.categoryType}"/>

</LinearLayout>

</layout>
